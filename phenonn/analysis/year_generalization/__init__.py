"""Temporal-generalization study: train/val on different YEAR splits (same sites,
OVERLAP mode) to measure how prediction skill changes when validating on the
future (chrono), on random years (rand1..4), or on the hottest years while
training on the coldest (coldhot). Compare ΔR² across splits."""
